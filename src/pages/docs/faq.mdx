---
layout: ../../layouts/DocsLayout.astro
title: FAQ
description: Verified answers about LocalCloud setup, compatibility, persistence, licensing, and troubleshooting.
author: LocalCloud
updated: 2026-08-13
reviewed: 2026-08-13
---

import FAQPageSchema from '../../components/FAQPageSchema.astro';
import { faqSections } from '../../data/faqContent';

<FAQPageSchema />

# FAQ

{faqSections.map((section) => (
  <section>
    <h2>{section.title}</h2>
    {section.entries.map((entry) => (
      <article>
        <h3>{entry.question}</h3>
        <p>{entry.answer}</p>
        {entry.code && <pre><code>{entry.code}</code></pre>}
        {entry.afterCode && <p>{entry.afterCode}</p>}
      </article>
    ))}
  </section>
))}

Related policy and implementation details are documented in [Licensing](/docs/licensing/), [Privacy](/docs/privacy/), [Architecture](/docs/architecture/), and the [service catalog](/services/).
